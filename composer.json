{
    "name": "snake14/php-vue-random-selector",
    "description": "This provides a nice little web app for randomly selecting things UI.",
    "require": {
        "bcosca/fatfree-core": "^3.7"
    },
	"autoload": {
        "classmap": [
            "src/"
        ]
    },
    "scripts": {
        "start": "php -S localhost:7009 -t public",
		"test": "./vendor/bin/phpunit tests"
    },
	"config": {
		"process-timeout": 36000
	},
    "require-dev": {
        "phpunit/phpunit": "^9"
    }
}
